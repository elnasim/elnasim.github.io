$(document).ready(function () {

  // -----------------------------------------------
  // PRELOADER
  // -----------------------------------------------

  function preloaderRemove() {
    $('.preloader').remove();
  }

  setTimeout(preloaderRemove, 2000);

});